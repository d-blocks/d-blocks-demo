-- View for PRODUCT_DIM
REPLACE VIEW {{env}}_tgt_v.PRODUCT_DIM AS
SELECT *
FROM {{env}}_tgt_t.PRODUCT_DIM
;


comment on VIEW "{{env}}_tgt_v"."PRODUCT_DIM" is '1:1 view for PRODUCT_DIM table, representing product details with historical tracking';